<h1>Movie Data Results Page</h1>
<a href="/">Go Back To Search</a>

<% movieData["Search"].forEach((movie) => { %>
    <ul>
        <img src=<%=movie["Poster"]%>>
        <br>
        <strong><%= movie["Title"] %></strong> - <%= movie["Year"] %>
    </ul>
<% }); %>

